# H2 Embedded Database Configuration
# No setup required - database file stored locally

db.url=jdbc:h2:./pbl_database;MODE=MySQL
db.user=sa
db.password=
db.driver=org.h2.Driver

# Connection Pool Settings
db.pool.minConnections=5
db.pool.maxConnections=20
db.pool.timeout=30000
